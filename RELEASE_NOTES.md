# Ledger Max 旗舰美学与智能分类版 v1.2.1

> 🛡️ **Max 旗舰版 v1.2.1** 是一个归档安全热修复版本。本版本在 v1.2 多文件夹监听能力基础上，强化“安全保险箱归档”的底层 Worker 防御，避免未来入口直接复用归档线程时误删目标 ZIP。

---

## 🔧 修复内容

- **修复 ZIP 自目标归档风险**：`ZipWorker` 内部会再次排除目标 ZIP 文件，避免源文件列表中包含目标 ZIP 时发生自删。
- **强化归档完整性校验**：先写入临时 ZIP，再执行 `testzip()` 校验；通过后才替换正式归档文件。
- **降低数据丢失风险**：只有成功写入并校验通过的源文件才会被清理，完成信号也会返回实际归档文件数量。

## ✅ 回归验证

- 正常归档两个同名文件：ZIP 内保留 `A/same.txt`、`B/same.txt`，源文件和数据库记录正确清理。
- 自目标边界：`ZipWorker([victim.zip], victim.zip)` 不再删除原 ZIP，会发出错误信号并保留原内容。
- Python 全量源码编译检查通过。

---

## 📦 安装说明

1. 下载 `Ledger-v1.2.1-windows-x64.zip` 绿色便携版，解压后运行 `Ledger.exe`。
2. 或下载 `Ledger-Setup-v1.2.1-windows-x64.exe`，按安装向导完成安装。
3. 首次启动会自动初始化工作空间和数据库。

## 💻 系统要求

- Windows 10 / 11 (64-bit)
- 无需安装 Python 或其他运行时环境

---

*Ledger Max 旗舰版 · 由 TimeTravelCoder 开发*
