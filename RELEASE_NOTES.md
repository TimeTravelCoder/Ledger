# Ledger Max 旗舰美学与智能分类版 v2.0.0

> Max 旗舰版 v2.0.0 是一个稳定性修复版本，重点解决打包版启动失败无提示、托盘关闭行为不清晰、后台任务退出时可能假死或损坏数据的问题。

---

## 修复内容

- 修复 Windows `--windowed` 打包后启动异常无提示的问题：启动失败时会弹出错误提示，并写入 `%APPDATA%\Ledger\ledger_crash.log`。
- 优化托盘行为：点击窗口关闭按钮会最小化到系统托盘，托盘菜单中的“退出”才会真正关闭程序。
- 移除退出时强制终止备份线程的高风险逻辑，避免复制文件或写入数据库时被硬杀。
- 优化备份任务生命周期，防止重复启动备份，并在任务完成后安全释放 worker。
- 优化安全保险箱归档任务生命周期，完成或失败后会关闭进度框并清理 worker 引用。
- 默认关闭主线程 Word COM 转 PDF 预览，避免坏 `.docx` 或 Word 卡住导致界面假死；如需启用，可设置环境变量 `LEDGER_ENABLE_WORD_COM_PREVIEW=1`。

## 验证结果

- Python 全量源码编译检查通过。
- 主窗口、系统托盘、主要页面无界面初始化测试通过。
- 窗口关闭到托盘、托盘恢复、托盘退出路径 smoke test 通过。
- `git diff --check` 检查通过。

---

## 安装说明

1. 下载 `Ledger-v2.0.0-windows-x64.zip` 绿色便携版，解压后运行 `Ledger.exe`。
2. 或下载 `Ledger-Setup-v2.0.0-windows-x64.exe`，按安装向导完成安装。
3. 首次启动会自动初始化工作空间和数据库。

## 系统要求

- Windows 10 / 11 (64-bit)
- 无需安装 Python 或其他运行时环境

---

*Ledger Max 旗舰版 · 由 TimeTravelCoder 开发*
